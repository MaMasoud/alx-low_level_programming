#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	for (int alpha = 'a'; alpha <= 'z'; alpha++)
	{
		putchar(alpha);
		putchar(10);
	}
	return (0);
}
